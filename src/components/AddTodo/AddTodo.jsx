import React from "react";

const AddTodo = () => {
  return (
    <div>
      <input type="text" placeholder="Enter the content" />
      <button>Add</button>
    </div>
  );
};

export default AddTodo;
